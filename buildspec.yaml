
version: 0.1
phases:
  install:
    commands:
      - aws cloudformation package --template-file setup-es-kibana.yaml --s3-bucket eslex-codepipelinebucket-qrqr8rh9s380
                                   --output-template-file outputSamTemplate.yaml
artifacts:
  type: zip
  files:
    - setup-es-kibana.yaml
    - outputSamTemplate.yaml
